{
  "name": "my-ci-cd-poc",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "lint": "eslint .",  // Add the lint script
    "test": "echo \"No tests specified\" && exit 0"  // Placeholder for tests
  },
  "dependencies": {},
  "devDependencies": {
    "eslint": "^7.32.0"  // Add ESLint as a development dependency
  }
}
